{
	"name": "<%= package %>",
	"version": "0.0.1",
	"dependencies": {
		"deliteful<% if (build) { %>-build<% } %>": "0.3.x",
		"requirejs": "2.1.x"<% if (!build) { %>,
		"requirejs-domready": "2.0.x"<% } %><% if (i18n) { %>,
		"requirejs-dplugins<% if (build) { %>-build<% } %>": "0.2.x-dev"<% } %>
	}
}